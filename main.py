if __name__ == "__main__":
    # start_new_game
    # insert_opponent_masteries to master
    # start draft - copies the graph
    # draft.py exposes suggest_pick, suggest_ban, pick(), ban()
    # graph.py exposes buld_master_graph(), build_match_graph()
    # mastery.py exposes build_masteries, set_mastery (sets to master graph)
